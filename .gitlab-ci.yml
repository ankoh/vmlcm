before_script:
  - mkdir -p /workspace/src/gitlab.kohn.io/ankoh/vmlcm
  - cp -r ./* /workspace/src/gitlab.kohn.io/ankoh/vmlcm
  - cd /workspace/src/gitlab.kohn.io/ankoh/vmlcm
  - export GOPATH=/workspace
  - go get -d -t -v ./...
  - go install -v

tests:
  type: test
  script:
    - go test -v -cover ./...
  tags:
    - golang
    - plain
